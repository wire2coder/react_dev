var React = require('react');

var ListItem = function(props) {

    let paObjects = props.paObjects;

    
    // let paList = paObjects.map( function(pa) {
    //     return  pa.paname;
    // } )

    // // return JSX
    return (
        <div>
            <ul>
                paList
            </ul>
        </div>
    );
}

module.exports = ListItem;